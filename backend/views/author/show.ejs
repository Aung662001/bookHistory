<h3>Author Name: <i><%= author.name %></i></h2>
<a href="/authors/<%= author.id %>/edit" class="btn btn-primary">Edit</a>
<%- include("../partials/delete_form.ejs",{url:`/authors/${author.id}`})%> <%
if(books.length > 0){ %>
<h3>Books By Author</h3>
<% books.forEach(book=>{ %>
<a href="/books/<%= book.id %>"
  ><img src="<%= book.coverImagePath %>" width="100" height="150"
/></a>
<%})%> <%}%>
